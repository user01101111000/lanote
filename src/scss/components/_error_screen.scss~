.error_screen {
  position: fixed;
  inset: 0;

  min-height: 100dvh;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.75);


  display: flex;
  justify-content: center;
  align-items: center;

  flex-direction: column;

  gap: 2rem;

  padding: 2rem;

  h1 {
    color: rgba(255, 255, 255, 0.6);
    font-size: clamp(1.7rem, 3vw, 2rem);
    text-align: center;
    line-height: 1.2;
  }

  .error_burn {

    mask-image: radial-gradient(circle, rgba(0, 0, 0, 0.9) 40%, rgba(0, 0, 0, 0) 100%);
    img{
      opacity: 0.8;
    }
  }

}