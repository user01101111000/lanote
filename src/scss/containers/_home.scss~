@use "../utils/mixins" as *;

.home {
  width: 100%;

  display: flex;
  justify-content: center;
  align-items: center;


  form {
    display: flex;
    justify-content: center;
    align-items: center;

    flex-direction: column;

    .loading_win {
      position: absolute;
      inset: 0;
      z-index: 5;

      height: 100%;
      width: 100%;

      background-color: rgba(0, 0, 0, 0.75);


      display: flex;
      justify-content: center;
      align-items: center;

      flex-direction: column;

      gap: 2rem;

      color: rgba(255, 255, 255, 0.8);


      h1 {
        font-size: clamp(1rem, 2.5vw, 1.8rem);
        font-weight: 500;
      }

      figure {
        height: auto;
        width: 200px;
        opacity: 0.7;
      }

      @include respond(phone) {
        figure {
          height: auto;
          width: 140px;


          img {
            height: 100%;
            width: 100%;
          }
        }

      }

    }


    figure {
      user-select: none;

    }

    .paper {
      position: relative;
      height: 700px;
      width: auto;


      img {
        width: 100%;
        height: 100%;

        filter: brightness(75%);
      }

      .home_content {
        display: flex;
        flex-direction: column;
        align-items: center;
        position: absolute;
        inset: 0;

        gap: 1rem;
        transform: scaleX(0.80) scaleY(0.70);

        button {
          background-color: transparent;
          border: none;
          outline: none;
          transform: rotate(-40deg);
          cursor: pointer;

          figure {
            height: 70px;
            width: 70px;

            filter: drop-shadow(0 0 1rem rgba(0, 0, 0, 0.8));


            img {
              height: 100%;
              width: 100%;
            }

            @include respond(tab-land) {
              height: 60px;
              width: 60px;
            }

            @include respond(tab-port) {
              height: 50px;
              width: 50px;
            }

            @include respond(phone) {
              height: 40px;
              width: 40px;
            }

          }
        }


        textarea {

          background-color: transparent;


          text-align: center;
          font-size: 3rem;
          font-style: italic;

          border: none;
          outline: none;
          line-height: 1.5;

          height: 100%;
          width: 100%;


          transform: rotate(1deg);
          scrollbar-width: none;

          font-weight: 500;

          mask-image: radial-gradient(circle, rgba(0, 0, 0, 0.7) 80%, rgba(0, 0, 0, 0) 100%);

          border-radius: 20px;


          &::placeholder {
            color: rgba(35, 35, 35, 0.5);
          }


          @include respond(tab-land) {
            font-size: 2.5rem;


          }

          @include respond(tab-port) {
            font-size: 2.2rem;

          }

          @include respond(phone) {
            font-size: 2rem;
          }


        }


        .error_message {
          position: absolute;
          bottom: 3.5rem;

          font-size: 1.5rem;
          padding: 0.7rem 0.8rem;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.75);

          border-radius: 10px;

          background: linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.4)), url(src/assets/images/error_bg.jpg);

          @include respond(tab-land) {
            font-size: 1.2rem;
            bottom: 3rem;

          }

          @include respond(tab-port) {
            font-size: 1rem;
            bottom: 2rem;
          }

          @include respond(phone) {
            font-size: 0.8rem;
            bottom: 1rem;
          }

        }
      }


      @include respond(tab-land) {
        height: 600px;


      }

      @include respond(tab-port) {
        height: 500px;
      }

      @include respond(phone) {
        height: 400px;
        width: auto;
      }


    }


  }

  .link_page {
    position: absolute;
    inset: 0;
    z-index: 8;
    min-height: 100dvh;
    width: 100%;


    overflow: hidden;

    display: flex;
    justify-content: center;
    align-items: center;

    flex-direction: column;

    gap: 2rem

    background-color: rgba(0, 0, 0, 0.75);


    .link_content {

      width: min(100%, 800px);
      padding: 1rem;

      mask-image: radial-gradient(circle, rgba(0, 0, 0, 0.9) 50%, rgba(0, 0, 0, 0) 100%);


      background-image: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)), url("/src/assets/images/link_bg.webp");;

      h1 {

        font-weight: 600;
        letter-spacing: 1.2px;
        text-align: center;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.8);

      }
    }

    .copy_button {
      position: relative;
      width: 170px;
      height: auto;

      opacity: 0.7;

      transition: all 0.3s ease-in-out;

      &:hover {
        opacity: 0.85;
      }


      img {
        height: 100%;
        width: 100%;


      }


      h1 {
        position: absolute;

        top: 47%;
        left: 50%;
        transform: translate(-50%, -50%);

        text-wrap: nowrap;


        display: flex;
        justify-content: center;
        align-items: center;


        cursor: pointer;
        font-size: 2rem;

        color: rgba(35, 35, 35, 0.7);
        font-weight: 500;


        @include respond(tab-land) {
          font-size: 1.7rem;
        }

        @include respond(tab-port) {
          font-size: 1.4rem;
        }

        @include respond(phone) {
          font-size: 1.2rem;
        }
      }

      @include respond(tab-land) {
        width: 140px;
      }

      @include respond(tab-port) {
        width: 130px;
      }

      @include respond(phone) {
        width: 120px;
      }


    }

  }


}
