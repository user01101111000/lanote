@use "../utils/mixins" as *;

.message {
  min-height: 100dvh;
  width: 100%;

  overflow: hidden;

  display: flex;
  justify-content: center;
  align-items: center;

  flex-direction: column;

  gap: 1.5rem;

  transition: all 0.3s ease-in-out;
  color: var(--text-color);
  background: linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.8)), url('/src/assets/images/bg.webp') center center no-repeat;
  background-size: cover;


  .paper {
    position: relative;
    height: 700px;
    width: auto;


    img {
      width: 100%;
      height: 100%;

      filter: brightness(75%);
    }


    .message_content {

      display: flex;
      align-items: start;
      justify-content: center;

      position: absolute;
      inset: 0;

      gap: 1rem;
      transform: scaleX(0.80) scaleY(0.70);


      background-color: red;
    }
  }

}